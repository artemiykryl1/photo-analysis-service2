# TASK-002.1 — Багфиксы по внешним ревью TASK-002

**Статус:** 📋 спека готова, конвейер НЕ запущен (ждёт команды пользователя).
**Спека:** `specs/feature-upload/tasks.md`, раздел TASK-002.1 (находки F1–F8, критерии приёмки, ограничения).
**База:** ветка feat/task-002-async-pipeline после PR #5 + пост-ревью коммит 3c0ccf5.

## Происхождение

Два внешних ревью после мёржа PR #5:
- Ревью 1 (ревьюер курса): сирота в MinIO при падении commit; catch-all в consumer ломает at-least-once; RAM-загрузка file.read; insecure gRPC.
- Ревью 2 (второе независимое): лимит батча стоит ПОСЛЕ материализации в RAM (фикс по 41_review-2 #6 не решил заявленную проблему — процессный урок для re-review); photos_pending задвоен (worker экспортирует Gauge=0, на живом скрине Grafana две серии «pending»); GET /v1/batches/{id} пишет в БД; нет restart-политик в compose.

**Верификация оркестратором (2026-07-21): все 8 находок подтверждены по коду.** Ложных нет.

## Процессный урок (для re-review в этом конвейере)

Фикс BLK «лимит суммарного размера» в TASK-002 был принят re-review-ом формально (лимит добавлен), но не по существу (проверка после материализации). В этой таске ревьюерам явно проверять: фикс решает ЗАЯВЛЕННУЮ проблему, а не только добавляет код.

## План конвейера

| Шаг | Агент | Артефакт | Статус |
|-----|-------|----------|--------|
| 1 | context-collector | 10_context.md | ✅ done — карта F1–F8, списки завязанных тестов, таблица импортов метрик |
| 2 | architect | 20_design.md (в т.ч. решение по F7 TLS) | ✅ done — 16 шагов; F7 = дефер TLS в TASK-003 (обоснован); ключевое: F1 требует consumer.seek() для реальной передоставки |
| 3 | coder | 30_impl.md + код (F1–F8) | ✅ done — 370 passed, ruff clean, compose OK |
| 4 | reviewer-1 ∥ reviewer-2 | 40/41 | reviewer-2 ✅ APPROVE (0 блокеров); reviewer-1 ❌ CHANGES_REQUESTED — 1 BLOCKING (B1: незащищённый seek роняет воркер при ребалансе, доказано прогоном) + M2 (суммарный лимит батча недостижим) |
| 4b | coder (итерация правок 1) | 30_impl.md | ✅ done — BLK-1, BLK-2, MIN-1, MIN-2 (агент прерван сетью до самопроверок; они и отчёт выполнены оркестратором) |
| 4c | reviewer-1 (re-review) | 40_review-1.md | ✅ APPROVE — B1 закрыт (4 сценария прогоном: loop выживает, commit не вызван, happy path не деградировал), 0 блокеров |
| — | **ГЕЙТ РЕВЬЮ ПРОЙДЕН** | 40 + 41 | ✅✅ оба APPROVE (opus + haiku) |
| 4d | оркестратор: спека | specs/feature-upload/tasks.md | ✅ зафиксировано изменение контракта 500→150 МБ (видимое клиенту: 413 на батчах 150–500 МБ) |
| 5 | test-writer | 50_tests.md | ✅ done — 425 passed, coverage 94%→98%, файлы новой логики F1–F5 покрыты на 100%, новых багов нет |
| 5b | coder (итерация правок 2) | 30_impl.md | ✅ done — m2/m3/m4 закрыты + 6 регрессионных тестов; 431 passed, coverage 98% |
| 6 | test-debugger | 60_debug.md | не потребовался (всё зелёное) |
| 7 | pr-publisher | 70_pr.md | ⏸ ждёт: живой прогон + явное разрешение пользователя |

## Итог по коду (TASK-002.1)
Все 8 находок двух внешних ревью закрыты; блокер B1 (падение воркера при ребалансе) найден внутренним ревью и закрыт; 3 остаточных не-блокера (m2/m3/m4) закрыты финальной итерацией.
Гейты: ruff clean · 431 passed / 1 integration skip · coverage 98% · compose config OK (8/8 restart).
Изменение публичного контракта — ровно одно: лимит суммарного размера батча 500→150 МБ (зафиксировано в specs/feature-upload/tasks.md).

## Открытый вопрос закрыт
reviewer-1 подтвердил правомерность правки 7 UUID-тестов: единственный продьюсер — outbox.py:68, публикует str(row.photo_id) из UUID-колонки PK, не-UUID в реальном сообщении физически невозможен. Пробел был в дизайне («Ожидаемые поломки»), не у кодера.
| 5 | test-writer | 50_tests.md | ожидает |
| 6 | test-debugger | 60_debug.md | при падениях |
| 7 | pr-publisher | 70_pr.md | после зелёных тестов И разрешения пользователя |

## Решения оркестратора (заход TASK-002.1)
- R1 архитектора ОДОБРЕНО: непарсируемый UUID в photo_id = poison-pill (постоянная ошибка, повтор бессмыслен — соответствует цели спеки, шире буквы).
- F7: принят дефер TLS в TASK-003 (реальный анализатор придёт из внешней сети — там TLS обязателен; в локальном compose одна docker-сеть).

## Проверки оркестратора после кодера (2026-07-21)
- F4 подтверждён инструментально: `import app.worker.main` → реестр воркера содержит только 6 своих метрик, ни одной api-метрики (photos_pending/http_requests_total/photos_uploaded_total/kafka_publish_errors_total отсутствуют).
- F3 подтверждён: в batch_service.py слова commit/try_complete встречаются ТОЛЬКО в пояснительных комментариях, записи в GET-пути нет.
- F1: MessageOutcome + consumer.seek() присутствуют.
- F6: 8/8 сервисов с restart: unless-stopped.

## Открытый вопрос для ревью (заявлен кодером)
Реализация R1 (UUID как poison-pill) сломала 7 тестов в tests/test_worker_consumer.py, использовавших плейсхолдеры "photo-1"/"p1"/"p2" вместо валидных UUID. Кодер починил механически (подмена на валидные UUID-литералы, ассерты и логика не тронуты), но пункт НЕ был в «Ожидаемых поломках» дизайна — ревьюерам подтвердить правомерность правки.

## Заметки для конвейера

- F1 меняет семантику consumer — существующие тесты consumer/offset ожидаемо потребуют правки (это НЕ регресс, зафиксировать в 30_impl).
- F3: завершение батча уезжает в worker (analysis_processor после терминала). Атомарность: UPDATE batches ... WHERE status='processing'. get_batch → строго read-only.
- F4: раскол metrics.py на metrics_api.py / metrics_worker.py; проверить импорты и дашборд.
- Миграций нет, контракты API не меняются.
